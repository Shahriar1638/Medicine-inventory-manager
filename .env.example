# MongoDB connection string for invoice persistence.
# Copy this file to .env.local (git-ignored) and set your URI.
# If this variable is missing, the app stays fully functional using localStorage only.
MONGODB_URI=your-mongodb-connection-string-here
